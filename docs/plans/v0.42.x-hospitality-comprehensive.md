# v0.42.x — Comprehensive Hospitality Overhaul

**Research sources**: Toast POS, Square for Restaurants, Chowbus, GoTab,
Oracle OPERA, Mews PMS, Little Hotelier, Cake POS, Lightspeed, Odoo
Restaurant. Cross-referenced GitHub reference schemas (swetababu,
cgoncalves94, ThecoderPinar). Kenyan context: nafasi.net "Accommodation
POS", Folio.co.

This is the single plan for every hospitality issue the user has
raised. Ship in the order below; each release is small enough to
verify + tag + push.

## Guiding principles (from research)

1. **Menu items are NOT retail products.** They live only in the
   hospitality Orders workflow. A retail POS should never see a menu
   item as a sellable SKU. What DOES appear in retail POS is the
   raw inventory item (unga, sukuma leaves, cola bottle) OR the sale
   line the menu item produces AFTER cook.
2. **Every menu item has a Recipe.** Selling "Ugali Sukuma" deducts
   200g flour + 100g sukuma + 20g oil + 2 tomatoes + 0.5 onion. Recipes
   are a BOM (Bill of Materials); production.ts already implements FEFO
   deduction, we just wire it to menu items.
3. **Party size is required, named customer is optional.** Every table
   open must record cover count for reporting; only room-charging or
   loyalty flows need a named customer.
4. **Auto-fire to kitchen by default.** Modern POS (Toast, Square,
   Chowbus) fire items to KDS the moment they're added. Manual "Send
   to kitchen" is opt-in for slow-fine-dining review flow.
5. **Order status auto-derives from item status.** When every item is
   ready → order is ready → Order Board flips to READY column.
   When every item is served → order is served.
6. **Room service = one-click charge-to-folio.** Room-service order
   type + room number → auto-links to the room's open folio; on
   `served` the items post as folio charges.
7. **Room hierarchy**: Property → Floors → Rooms → Room Types.
   Housekeeping status is per-room. Reservations attach a guest to a
   room for a date range.

## Diagnosis (from code inspection)

| Symptom | Root cause | Fix |
|---|---|---|
| KDS + Order Board show nothing | `addOrderItem` inserts as `new`; user must click Send | Auto-fire on add (already coded, v0.42.0) |
| Order stays "sent" even after items ready | No `refreshOrderStatus` after bump | Coded in v0.42.0 |
| Ugali Sukuma appears in POS retail grid | `createMenuProduct` inserts kind='menu_item' but POS filter doesn't exclude | POS grid excludes kind='menu_item' |
| No recipe → no inventory deduction on serve | No BOM link from menu_items to raw products | Migration 076 + Recipe dialog + wire production.ts |
| Menu tiles don't show images | No `image_path` column | Migration 075 + upload (v0.42.0 coded) |
| Cover count not enforced | openOrder doesn't ask | Add party_size to opendOrder + require dialog |
| Native Windows titlebar strip visible | Variant configs replaced base `windows[0]` without `decorations` | Fixed in variant confs (this batch) |
| Windows 11 snap-layouts lost | `decorations: false` disables OS button | Rust WM_NCHITTEST or community plugin (v0.42.4) |

## Data model additions

### Migration 076 — recipes

```sql
CREATE TABLE IF NOT EXISTS recipes (
  id TEXT PRIMARY KEY,
  menu_item_id TEXT NOT NULL REFERENCES menu_items(id) ON DELETE CASCADE,
  yield_qty REAL NOT NULL DEFAULT 1,          -- one recipe → one plate by default
  notes TEXT,
  created_at TEXT NOT NULL DEFAULT (datetime('now'))
);
CREATE TABLE IF NOT EXISTS recipe_ingredients (
  id TEXT PRIMARY KEY,
  recipe_id TEXT NOT NULL REFERENCES recipes(id) ON DELETE CASCADE,
  product_id TEXT NOT NULL REFERENCES products(id),
  quantity REAL NOT NULL,
  unit TEXT NOT NULL DEFAULT 'g',             -- g / ml / pcs
  optional INTEGER NOT NULL DEFAULT 0,        -- e.g. lemon garnish
  sort_order INTEGER NOT NULL DEFAULT 0
);
CREATE UNIQUE INDEX IF NOT EXISTS idx_recipe_menu ON recipes(menu_item_id);
CREATE INDEX IF NOT EXISTS idx_recipe_ing_recipe ON recipe_ingredients(recipe_id);
```

### Migration 077 — floors (optional grouping)

```sql
CREATE TABLE IF NOT EXISTS floors (
  id TEXT PRIMARY KEY,
  branch_id TEXT,
  name TEXT NOT NULL,                          -- "Ground", "1st Floor"
  sort_order INTEGER NOT NULL DEFAULT 0,
  created_at TEXT NOT NULL DEFAULT (datetime('now'))
);
-- rooms.floor is already a free text column; add floor_id for structured pick
ALTER TABLE rooms ADD COLUMN floor_id TEXT REFERENCES floors(id);
```

### Existing (already in place)

- `menu_items` (v0.075 added `image_path`, `allergens`)
- `hospitality_orders` (status: open/sent/preparing/ready/served/paid/voided)
- `hospitality_order_items` (status same, `sent_at`, `ready_at`, `served_at`)
- `rooms`, `room_types`, `bookings`, `guest_folios`, `folio_charges`
- `production_boms` + `bom_ingredients` (exists but unused — could reuse
  instead of a new `recipes` table; will decide during v0.42.2)

## Ship order

### v0.42.0 — Foundations (in progress → ship next)

- Migration 075: menu_items.image_path + allergens
- addOrderItem auto-fire on default setting (`hospitality.auto_fire = on`)
- refreshOrderStatus called on bump/serve/void so order status derives
  from item statuses
- Menu-item photo upload in MenuItemDialog (data-URL, max 1MB)
- Native decoration fixes: variant conf files force `decorations: false`
  + `shadow: false`; secondary Tauri windows same
- Retire back button from KDS header (was ugly)
- Logo wiring through receipts + PDFs (already in v0.41.5)
- Bundle ready to ship.

### v0.42.1 — Menu items exit the POS retail grid

- POS product-grid query filters `kind != 'menu_item'`.
- Menu items only appear in `/hospitality/orders` tile grid.
- Migration adds `products.kind` index if missing.
- Test: create "Ugali Sukuma" as menu item, confirm it's absent from
  `/pos/sale` product grid + `/inventory/products` list.

### v0.42.2 — Recipes (menu items ↔ inventory)

- Migration 076 (recipes + recipe_ingredients).
- New "Recipe" tab on MenuItemDialog: pick ingredients from products
  with quantity + unit; running total costs computed live.
- When order item transitions to `served`: fetch its menu_item → recipe
  → for each ingredient, deduct FEFO from batches via
  production.ts::deductIngredients (rename existing production BOM logic
  if generic enough).
- Recipe-costing report: KES cost / plate for every menu item.

### v0.42.3 — Party size + room service

- `openOrder({ tableId, orderType, partySize })` requires partySize for
  dine_in + room_service.
- "New order" dialog: table picker + party-size stepper (1-30) +
  optional customer combobox.
- Room-service: room picker → auto-links to `guest_folios` row for
  that room's current booking. If no open folio → error toast.
- On order `served`, post its items to `folio_charges` for room-service.
- Runner view (in Hospitality hub) filters to
  `order_type = 'room_service' AND status = 'ready'` with room number
  prominent.

### v0.42.4 — KDS + Order Board content correctness

- KDS ticket shows: item name + photo (32px) + qty + modifiers + notes
  + allergen pills (rose when present) + elapsed time.
- Modifiers loaded from `hospitality_order_items.modifier_total` + a
  new join to `menu_modifier_selections` (add if missing).
- Order Board shows: order number, table code (or Room 204 badge),
  guest name if present, party size, elapsed time. No item names on
  Order Board — that's for the customer-side, not detail.
- Customer name flows: on order open with named customer, denormalise
  into `hospitality_orders.customer_id`; queue query joins to
  `customers.name`.
- Real-time: bump refresh interval to 3s (currently 5s KDS, 2s queue);
  add a websocket-style store push if trivial to layer on top.

### v0.42.5 — Floors + rooms drill-down improvements

- Migration 077 (floors table + rooms.floor_id).
- Floors CRUD in Hospitality hub Rooms tab.
- Room-status board groups by floor (Ground / 1st / 2nd) with per-floor
  KPI strip (occupied / free / dirty).
- Room detail page (v0.40.3 exists) gets a "Guests during stay"
  breakdown + "Housekeeping notes" section.

### v0.42.6 — Windows 11 snap layouts on custom titlebar

Two options; we pick the safer one:

**Option A (safe)**: use `tauri-plugin-snap-layout` community crate.
  - Cargo dep: `tauri-plugin-snap-layout = "0.3"`
  - Register the plugin in lib.rs
  - Attach `data-snap-layout` to our themed maximize button
  - Native Win32 overlay handles hover
  - Fallback on non-Win11 platforms (no-op)

**Option B (hand-roll)**: Write a small Rust plugin overriding
  `WM_NCHITTEST` to return `HTMAXBUTTON` when hovering our custom
  button. Needs `windows-sys` crate for message loop hooks. Higher
  maintenance cost.

**Decision**: try Option A first. If the crate is stale/broken, fall
back to Option B.

## Non-scope

- Third-party delivery integrations (Doordash, Uber Eats) — no Kenyan
  presence yet
- Channel manager for OTAs (Booking.com, Airbnb) — separate v0.43+
- Time-of-day menus (breakfast / lunch / dinner cutoffs) — v0.43+
- Multi-property (chain) — Omnix is per-branch by design
- Kitchen printer routing per station — deferred (already have
  `kitchen_stations.printer_name` column, wire in later)
- QR-code table ordering — v0.43+

## Verify gate (per sub-release)

Same as every release:
1. `npx tsc --noEmit` clean
2. `npx vitest run --silent` all green
3. `node scripts/audit-codebase.mjs` 0 errors
4. Cold-cache `next build` in website/ clean
5. Tag + push
6. Rebuild Tauri binary before user tests any window-config change

## Testing checklist (once shipped)

- Menu: create "Ugali Sukuma" — appears in Hospitality menu, absent
  from `/pos/sale`, absent from `/inventory/products`
- Recipe: attach 200g ugali + 100g sukuma to "Ugali Sukuma" —
  ingredient stock deducts when the order is served
- Order: open table T-4 with 3 guests, add 2× Ugali Sukuma; items
  auto-fire; KDS shows ticket immediately; Order Board shows
  "T-4 · 3 guests · #123 · PREPARING"
- Chef: bump both items on KDS; order status → ready; Order Board
  flips to READY column
- Runner: mark items served; ingredient stock decremented; folio
  charged (room-service) OR order goes to POS for payment
- Windows: hover the maximize button → OS snap-layouts popup appears
- Native chrome: no double titlebar; F11 fullscreens the KDS

---

# Extension — v0.43.x (research complete, planning only)

Added 2026-07-03 based on direct user feedback in the same session:

1. **Titlebar has no module icon or module name** — it's an empty strip.
2. **Settings left rail** — bottom is empty; should show module logo +
   "Powered by Omnix".
3. **Hardware "New quote" toast** — should redirect to the actual
   builder, not tell the user to "go to POS".
4. **Menu items need a real drill-in** — dedicated detail page with
   pagination, mark-unavailable/86, and a full visual recipe builder
   (curved lines linking ingredients), not the current chip UI.
5. **v0.44+ non-scope** — restated so we don't lose them:
   menu-item variant pricing, recipe versioning, batch recipes,
   nutrition info.

## v0.43.0 — Custom titlebar shows module identity + no more dead-end toasts

### Titlebar identity strip

Current `WindowTitlebar` (`src/components/layout/window-titlebar.tsx`):
- Left = 96 px empty spacer
- Center = optional string prop, unused for the main app
- Right = window control buttons

Turn the left spacer into a **module identity block**:

```
┌────────────────────────────────────────────────────────┐
│ [🍽] Omnix Hospitality · Orders             — □ ×     │
└────────────────────────────────────────────────────────┘
   ^     ^               ^                    ^
   icon  variant name    current-route label  controls
```

Rules:
1. **Icon** — 16 px module icon on far left. Icon source:
   - For the fixed-variant builds (Dawa / Retail / Hospitality /
     Hardware): a static icon per variant (`Pill` / `Storefront` /
     `ForkKnife` / `Wrench` from Phosphor).
   - For the Pro build: use `useActiveModule()` from
     `src/stores/active-module.ts` — icon flips as the user changes
     module.
   - Icon colour = the variant accent (`VARIANT_ACCENT` from
     `src/lib/variant.ts`).
2. **Product name** — `BRAND.name` from `src/lib/brand.ts` (e.g.
   "Omnix Hospitality"). Font: display, size 12 px, tracks -0.005em.
3. **Route label** — small `·` separator then the current top-level
   route (Orders / Rooms / Menu / …). Derived from `useLocation()` and
   a small `ROUTE_LABEL_MAP`. Muted 11 px.
4. **Draggable** — the whole strip keeps `data-tauri-drag-region` so
   the window drags from anywhere.

**Fullscreen (KDS + customer display) exceptions**:
- KDS + Order Board pass their own `title` prop and don't show the
  module logo (they run in dedicated windows). Existing behaviour.

### Toasts → redirects: sweep

Anti-pattern in `src/pages/hardware.tsx:135`:
```tsx
onClick={() => toast.info("Use POS → Quote, or the contractor account page to build a quotation.")}
```

Convert to a proper redirect. Two sub-tasks:

1. **Frontend redirect** — button becomes:
   ```tsx
   onClick={() => navigate("/pos/sale?mode=quote")}
   ```
2. **POS quote-mode**:
   - `usePosCartStore` gets a `quoteMode: boolean` flag.
   - When quote mode is on:
     - Top of `/pos/sale` shows a slim yellow banner "Quotation mode —
       nothing will be sold; you'll save a quote instead."
     - "Charge" button relabels to "Save quote".
     - Payment flow is replaced with a lightweight "Save quote as…"
       dialog that saves via `createQuotation` on `hardware.ts`.
     - On save, navigate back to `/hardware/quotations` and open the
       new quote's detail page.
   - Read `mode` from `useSearchParams()` on mount, set the flag.
   - Add "Quotation mode" toggle to the POS toolbar so a user can
     switch after arriving.

**Sweep for other instances**:
```bash
grep -rE 'toast\.(info|warning|success)\("(Use|Go to|Head to|Try|Open|From the|From your)' src/
```
Only one hit currently — but re-run after every batch to catch
regressions. Add a lightweight audit rule
(`scripts/audit-codebase.mjs`) that flags any `toast.info` referencing
a route path (`/x`).

### Settings sidebar footer

`src/components/layout/settings-layout.tsx` — `aside` between nav
and bottom is empty. Add a bottom-anchored block:

```
┌──────────────────────┐
│  ...settings nav...  │
│                      │
├──────────────────────┤
│  [module icon]       │
│  Omnix Hospitality   │
│  Powered by Omnix    │
│  v0.43.0             │
└──────────────────────┘
```

- Module icon (32 px) + name (`BRAND.name`) in the display font.
- `Powered by ${BRAND.parentBrand}` in muted 10 px — only when the
  variant is not Pro (Pro doesn't need to be "powered by itself").
- Version pill (linked to `/settings/updates`).
- The block is inside the `aside`, `mt-auto` after the nav to
  push it to the bottom.

## v0.43.1 — Menu-item detail page + row-click routing

New route: `/hospitality/menu/:id` — `MenuItemDetailPage`.

Sections top-down (all present but built up in later releases):

1. **Header block** (this ship)
   - Hero image (128 px square, drop-shadow, image upload inline).
   - Name (inline-editable via `EditableText`).
   - Category chip + Station chip (inline-editable comboboxes).
   - Prices: dine-in / takeaway (inline-editable currency inputs).
   - Prep time (min).
   - Allergen chips (add/remove).
   - Active toggle.
   - **86 toggle** — this ship: simple boolean; upgrade to expiry in
     v0.43.2.
   - "Duplicate" button.
   - Sticky "Save" button that batches all changes.

2. **Availability strip** (this ship, read-only)
   - Live max servings from `menuAvailability()`.
   - Bottleneck ingredient name + current stock.
   - "This is stopping you selling more" callout when stock=0.

3. **Recipe section** (placeholder in this ship)
   - Shows the current chip/table `RecipeDialog` inline.
   - Big "New visual builder →" button that will land in v0.43.3.

4. **Sales trend** — deferred to v0.43.5.

5. **Modifiers** — deferred to v0.43.4.

Row click on `/hospitality` Menu tab → `navigate("/hospitality/menu/${id}")`.

## v0.43.2 — Menu list pagination + search + bulk 86

- New service helper `pageMenuItems({ search, category, page, pageSize })`
  in `src/services/paged.ts`. Uses the standard `Paginated<T>`
  contract.
- Menu tab wires `PaginationBar` + search input above the table.
  Category filter chips beside search.
- Row-select checkboxes on hover. Shift-click multi-select. Toolbar
  action bar appears above the table when N ≥ 1 rows selected:
  "Mark unavailable · Mark available · Duplicate".
- **Soft-86 with expiry**: new migration.
  ```sql
  CREATE TABLE IF NOT EXISTS menu_86s (
    menu_item_id TEXT PRIMARY KEY REFERENCES menu_items(id) ON DELETE CASCADE,
    reason TEXT,
    until TEXT,   -- ISO datetime; NULL = indefinite until manually restored
    set_by TEXT REFERENCES users(id),
    set_at TEXT NOT NULL DEFAULT (datetime('now'))
  );
  ```
- Menu queries left-join `menu_86s` and treat items where
  `until IS NULL OR until > datetime('now')` as unavailable.
- Detail page header 86 toggle prompts for optional expiry ("Back
  after: 6pm | tomorrow | end of week | indefinite").
- KDS + Order-taking grid already have availability logic — extend it
  to check `menu_86s`.

## v0.43.3 — Visual recipe builder (React Flow / xyflow)

Replaces the chip `RecipeDialog` in the detail page. Chip UI stays as
`?fallback=1` opt-in for narrow screens.

### Library

```json
"@xyflow/react": "^12.4.4",
"@dagrejs/dagre": "^1.1.4"
```

MIT-licensed, ~50k stars, TS-native, mature. `dagre` handles
left-to-right DAG auto-layout.

### Component: `RecipeCanvas`

- Path: `src/components/hospitality/recipe-canvas.tsx`
- Props: `{ menuItemId, menuItemName, menuItemImage, sellingPrice }`
- Loads recipe via `getRecipeForMenuItem` (already added v0.42.2).

### Node types

- **DishNode** — center-right, larger card, photo + name +
  cost/serving (live) + suggested selling price at 65% food-cost.
  Border colour = margin health (green >60%, amber 30–60%, red <30%).
- **IngredientNode** — draggable, small card, product name + qty +
  unit + waste% + running cost. Border colour = stock health
  (green comfortable, amber below reorder level, red empty).
- **StationNode** — coloured backdrop that groups ingredients by prep
  station. User drops ingredients on it to assign station.
- **ModifierNode** (v0.43.4) — floats above the dish. Optional add-on
  flow — visual proxy for a modifier group.

### Edges

- Curved bezier from IngredientNode → DishNode.
- Edge label = qty + unit (e.g. "200g", "0.5 pcs").
- Solid dark stroke = required; dashed amber = optional.

### Toolbar

- **+ Ingredient** — product combobox drop-in; new node lands to the
  left of the dish, edge auto-connected.
- **Auto-layout** — dagre left-to-right, respects station grouping.
- **Preview cost** — recomputes total from graph.
- **Save recipe** — persists via `replaceRecipe` + writes canvas layout.

### Right sidebar (collapsible)

- Cost / plate (live).
- Selling price − cost = margin (colour-coded).
- Suggested price at 65% food-cost.
- Missing ingredients callout.

### Layout persistence

Migration 077:

```sql
ALTER TABLE recipes ADD COLUMN canvas_layout TEXT;
-- JSON: { nodes: [{ id, x, y, station_id? }], viewport: { x, y, zoom } }
```

`replaceRecipe` accepts + stores the JSON. `getRecipeForMenuItem`
returns it.

### Fallback

Screens <640 px auto-collapse to the existing chip UI. Detail page
has a "View chip editor" toggle for accessibility.

## v0.43.4 — Modifier groups CRUD

Schema (add if missing):

```sql
CREATE TABLE IF NOT EXISTS menu_modifier_groups (
  id TEXT PRIMARY KEY,
  menu_item_id TEXT NOT NULL REFERENCES menu_items(id) ON DELETE CASCADE,
  name TEXT NOT NULL,       -- "Sauces", "Sides"
  min_select INTEGER NOT NULL DEFAULT 0,
  max_select INTEGER NOT NULL DEFAULT 1,
  sort_order INTEGER NOT NULL DEFAULT 0
);
CREATE TABLE IF NOT EXISTS menu_modifier_options (
  id TEXT PRIMARY KEY,
  group_id TEXT NOT NULL REFERENCES menu_modifier_groups(id) ON DELETE CASCADE,
  name TEXT NOT NULL,       -- "Chilli", "Chapati"
  price_delta REAL NOT NULL DEFAULT 0,
  active INTEGER NOT NULL DEFAULT 1,
  sort_order INTEGER NOT NULL DEFAULT 0
);
```

Detail-page "Modifiers" section — nested list. Add group / add option
with price delta. Order-taking dialog surfaces modifier picker when
the menu item has groups. KDS ticket shows selected modifiers under
the item name.

## v0.43.5 — Sales trend + cost history charts

- **Sales trend** — 14-day stacked bar of units sold per day, split by
  order type (dine-in / takeaway / room-service). Data source:
  `sale_items` joined to `sales` joined to `hospitality_orders`.
- **Cost history** — line chart of per-serving cost over time. Each
  goods-receipt that adjusts the buying price of an ingredient
  recomputes the recipe's cost via a `recipe_cost_history` table.
- Both charts use `recharts` (already installed).

## v0.44+ non-scope reminder (retained)

- Menu-item variant pricing (small / medium / large plates).
- Recipe versioning (Toast has this — see historical costs).
- Batch recipes (5L stew feeds ~20 servings, batch cost + per-serving).
- Nutrition info per serving.
- Windows 11 snap-layouts (deferred from v0.42.6 — needs a mature
  plugin, current option `tauri-plugin-window-controls` is 0 stars).
- Structured floors table (`ALTER TABLE rooms ADD COLUMN floor_id`
  drafted, held for the housekeeping board expansion).

## Verify gate (unchanged)

Same as prior sub-releases:
1. `npx tsc --noEmit` clean
2. `npx vitest run --silent` all green
3. `node scripts/audit-codebase.mjs` 0 errors
4. Cold-cache `next build` in website/ clean
5. Tag + push
6. Rebuild Tauri binary before user tests any window-config change

## Testing checklist — v0.43.x

- Titlebar shows the fork+knife icon + "Omnix Hospitality · Orders"
  when on the orders page; icon accent = variant accent.
- Hardware Quotations "New quote" button → lands on POS with the
  yellow "Quotation mode" banner; "Charge" is now "Save quote".
- Menu list has search + pagination + bulk-select checkboxes; bulk
  "Mark unavailable" 86's 3 items with a chosen expiry, they disappear
  from Order-taking grid + KDS until expiry.
- Menu row click opens the detail page; header inline-edit round-trips
  values back to DB on Save; hero image upload works.
- Visual recipe builder loads with existing recipe as pre-laid graph;
  dragging repositions nodes; auto-layout snaps to dagre flow;
  Save persists ingredients + layout; reload shows same layout.
- Missing-ingredient node shows red; margin sidebar updates live.
- Modifiers: add "Sauces" group with two options, order-taking dialog
  now prompts for a sauce; KDS ticket shows the picked sauce.
- Sales trend chart shows correct daily totals for 14 days.

---

# Critical clarifications (2026-07-03, from user)

## Hospitality is a different beast from Dawa / Retail / Hardware

The trade modules split into two mental models:

**"Shelf" modules — Dawa, Retail, Hardware**
- Sell **discrete SKUs** off a shelf.
- Inventory tracks quantity per SKU per branch.
- A sale line deducts 1 unit of that SKU from stock.
- Everything Omnix already does — POS grid, product detail, stock take, PO
  receive, batches, expiry — was built for this world.

**"Composition" module — Hospitality**
- Sells **menu items** that are recipes/compositions of ingredients.
- **The menu item itself has no stock**. Selling "Ugali Sukuma" doesn't
  deplete a "1 × Ugali Sukuma" — it deducts 200 g flour + 100 g sukuma
  + 20 g oil, etc., from raw-ingredient stock (which is normal inventory).
- Availability is derived, not stored: max servings = min over
  ingredients of `floor(stock / recipe_qty)` — the bottleneck.
- 86 (make unavailable) is a **soft, temporary** hide — the item still
  exists, its recipe still exists, its ingredients still have stock;
  the kitchen has decided not to sell it right now.
- Rooms are not stock either — they have status
  (available / occupied / dirty / maintenance) and folios (running tabs).
- Recipes bridge the two worlds: menu-item sale → recipe lookup →
  ingredient FEFO deduction. `planRecipeConsumption` (already wired) is
  the exact glue.

**Implications for v0.43.x tasks that were being over-generalised**:

1. **Menu-item detail page must NOT show "stock quantity"**. It shows:
   - Availability strip (max servings from bottleneck ingredient).
   - Recipe (the composition).
   - Sales trend (how many were ordered, not how many "in stock").
   - Cost history (recipe cost changes as ingredient prices change).
2. **"86" is UI-level unavailability**, distinct from `active=0`. Active
   means "retired forever, hide from menus". 86 means "we've run out
   tonight, back tomorrow". Separate columns, separate flows.
3. **Menu pagination + search** is over `menu_items` (not `products`).
   POS product-grid pagination already exists and correctly excludes
   `kind='menu_item'` from v0.42.1.
4. **The visual recipe builder is the primary inventory-adjacent flow
   for a menu item** — it's how the operator connects a menu item
   ("Ugali Sukuma") to its raw ingredients (unga, sukuma, oil, salt).
   Adding an ingredient in the builder is picking a product from
   `products` (`kind='physical'`), never another menu item.

## Release strategy — build everything, tag once

- No intermediate version bumps between v0.43.0 and v0.43.5.
- Build all of the v0.43.x scope in one push, verify once at the end,
  then bump to v0.43.0 and tag/push a single release.
- Only exception: if a change is a critical hotfix (e.g. tsc-blocking
  regression discovered mid-flight) it may bounce out as a v0.42.6+
  patch before rejoining the v0.43.x work.

Applies to: titlebar identity, settings footer, quote-mode redirect,
menu-item detail page, menu list pagination + 86, visual recipe
builder, modifier groups, and the two detail-page charts.

---

# Final analysis + additions before starting v0.43.x execution

## Design principles for this batch (frontend-design skill)

Every new surface in v0.43.x follows the existing palette-scoped
system — no cliché cream+serif+terracotta, no drop shadows, no
gradients, no emoji. Concretely:

- Use `bg-background`, `text-foreground`, `border-border`,
  `bg-muted`, `text-muted-foreground` — never `bg-stone-*` or hex
  greys.
- Accent colour flows from `VARIANT_ACCENT` (already read from
  `src/lib/variant.ts`) — matches the module identity strip on the
  new titlebar.
- Slide-out `<Sheet>` from `@/components/ui/sheet` for data entry
  flows (Save-quote, 86 with expiry, modifier CRUD) — per AGENTS.md
  rule "Modal dialogs for data entry (use slide-out panels)".
- Copy in active voice: **"Save quote"** not "Submit"; **"Available
  today: 12 servings"** not "Stock: 12"; **"Back after 6 pm"**
  not "Unavailable until 18:00:00".
- One accent per surface — the yellow quote-mode ribbon is the
  boldest thing on `/pos/sale` when quote mode is on; everything
  else stays quiet.

## Order Board palette regression (existing bug uncovered by user)

`src/pages/customer-display-queue.tsx` uses hardcoded
`bg-stone-950 text-stone-100 border-stone-800 bg-stone-900/60`.
That was OK when there was one theme but now we have 10 palettes +
light/dark axis — the Order Board ignores the user's choice.

**Fix scope** (bundled into v0.43.0):
- Swap every `stone-*` class for theme tokens.
- Keep the "customer-facing display" high-contrast intent by using
  `bg-card`, `bg-muted`, `text-foreground`, `text-muted-foreground`
  — those already resolve dark under dark themes, light under light.
- KDS is already themed via tokens (already fine — verified).

**Bug class**: `hardcoded-stone-in-display-surfaces` — add an audit
rule so a future regression is caught.

## Additions to the POS quote-mode surface (from analysis)

- **Slim yellow ribbon** at top of `/pos/sale` while
  `quoteMode=true`. `bg-amber-500/15 border-b border-amber-500/30
  text-amber-900 dark:text-amber-300` — matches the palette in both
  modes.
- Content: "Quotation mode · nothing will be sold" (left) · customer
  name chip if selected · "Exit" button (right) that clears
  `quoteMode` + navigates to `/hardware/quotations`.
- **Pay button becomes "Save quote"** in the OrderSummary
  component — pass `quoteMode` down as a prop; button copy +
  `variant` swap accordingly.
- **`SaveQuoteDialog` is a Sheet**, not a modal. Right side,
  600 px wide.
  - Customer combobox (**required** — errored border if empty).
  - Cart summary chip: "12 items · KES 45,600".
  - Validity date picker (default today + 30d).
  - Notes textarea ("Any conditions, terms, delivery timeline").
  - Footer buttons: "Save quote" (primary) + "Cancel".
- On save → `confirm` dialog "Save this cart as a quote for
  {customer}? No payment will be taken." → `createQuotation` →
  navigate to `/hardware/quotations/:id` → `clear()` cart.

## Additions to menu-item detail page (from analysis)

- No "Stock qty" header stat — instead:
  **"Available today: {N} servings · Bottleneck: {ingredient} ({qty} on hand)"**.
- `active` toggle (retired forever) vs `86` toggle (out tonight,
  with expiry) are **two distinct pills**, not one. Different
  colour, different tooltip.
- Photo upload = existing FileReader pattern from
  `MenuItemDialog` — extract into `<PhotoUpload>` helper for reuse
  on the detail page and future modifier-option icons.
- Inline-edit UX: `EditableText` component (name, category, prices,
  prep, allergens) that swaps to `<Input>` on click, saves on Enter
  / blur, cancels on Escape. Reuse across every column.
- Sticky "Save changes" footer bar when any field is dirty —
  batches the edits into one `updateMenuItem` call.
- "Duplicate" opens `MenuItemDialog` with the current item prefilled
  as a starting point.

## Additions to the visual recipe builder (from analysis)

- **Ingredient picker only shows `kind='physical'` products** — the
  visual builder can never accidentally reference another menu
  item. Explicit `WHERE p.kind = 'physical'` in the picker query.
- **Bottleneck ingredient is highlighted** — the ingredient node
  whose stock divided by recipe qty gives the smallest number gets
  a subtle badge "🔥 bottleneck" (no emoji — use a Phosphor
  `Warning` icon instead per AGENTS.md).
- **Recipe cost updates live** as the operator changes qty / adds /
  removes ingredients; sidebar recalculates on every change.
- **Auto-layout** button uses `@dagrejs/dagre`. Renders ingredients
  on the left, dish on the right, curved bezier edges. Applied on
  first-load when `canvas_layout` is NULL.

## Additions to bulk 86 (from analysis)

- Expiry presets in the toolbar: "6 pm today" · "Tomorrow morning"
  · "End of week" · "Indefinite" — pre-computes the `until`
  datetime from the label so users don't have to think about time
  zones.
- Reason field is optional — a chef often knows why they 86'd, but
  it's not required for the row to save.
- 86 badge on menu tiles in order-taking + KDS: small red pill
  "86 — back after 6 pm" so waitstaff immediately know why they
  can't add it.

## Additions to hospitality inventory-adjacent flow (from analysis)

- **`Menu tab` in Hospitality Hub gets 2 stat pills at the top**:
  - "Available: N of M" (M = active menu items, N = items whose
    recipe has enough ingredient stock right now).
  - "86'd: K" (soft-unavailable count) — click to filter table.
- **Bottleneck report** — quick action on the Menu tab: "Show
  bottleneck ingredients" opens a Sheet that lists ingredients
  whose stock is the limiting factor for at least one menu item,
  sorted by number of menu items affected. Points the operator
  at what to reorder first.
- **Un-86 on ingredient receive** — when a goods receipt increases
  an ingredient's stock, auto-clear any `menu_86s` rows whose only
  reason was "out of X ingredient" (best-effort; requires a
  `reason_code` column, not just free-text reason, so defer to
  v0.44+).

## Consolidated task list for v0.43.x

Bundled into ONE tag at the end (no intermediate versions):

1. **v0.43.0** — Titlebar identity strip + settings sidebar footer +
   Order Board palette regression fix.
2. **v0.43.0** — Hardware "New quote" toast → navigate; POS
   quote-mode ribbon + Save-quote Sheet + button relabel + toolbar
   exit chip.
3. **v0.43.0** — Audit rule flagging `toast\.(info|warning)` that
   references a route path.
4. **v0.43.1** — `MenuItemDetailPage` shell at `/hospitality/menu/:id`,
   with hero image, inline-edit header, dual `active`+`86` pills,
   availability strip, embedded chip recipe placeholder.
5. **v0.43.1** — Menu row click routes to detail page.
6. **v0.43.2** — Menu list search + category filter chips +
   `pageMenuItems` server-side pagination + `PaginationBar` +
   row-select checkboxes + bulk 86 toolbar.
7. **v0.43.2** — Migration 077 `menu_86s` + LEFT JOIN in
   order-taking + KDS + expiry preset picker.
8. **v0.43.3** — Install `@xyflow/react` + `@dagrejs/dagre`,
   `RecipeCanvas` component with DishNode + IngredientNode +
   StationNode + curved edges + toolbar + right sidebar; migration
   078 `recipes.canvas_layout`; replaces chip UI on detail page
   (chip kept as `?fallback=1` for <640 px).
9. **v0.43.4** — Migration 079 `menu_modifier_groups` +
   `menu_modifier_options`; detail-page CRUD; order-taking picker;
   KDS ticket shows selections.
10. **v0.43.5** — Sales trend + cost history charts; new
    `recipe_cost_history` table populated by goods-receipt hook.

## Ship rhythm (unchanged from user's revised policy)

- Build every task above.
- Verify once at the end: tsc + vitest + audit + website build.
- Bump to v0.43.0.
- Tag + push a single release.

---

# v0.44.x — Full hospitality audit remediation

Written 2026-07-04 after a comprehensive audit prompted by real user
usage. The audit surfaced 30+ concrete issues; grouped into 4 atomic
sub-ships below. Every task references a schema/file/service so
execution is deterministic.

## v0.44.0 — Bookings + rooms end-to-end (highest impact)

The room-service, booking, and check-in flows are the biggest gap. A
front desk operator cannot:
- Pick a specific room during booking (only room type)
- Understand why "no available room" fires (silent status filter)
- Attach a room-service order to a specific room at open time
- Record guest details Kenya AHRA compliance requires (phone, ID)

### Data model additions

**Migration 080 — guests get more fields**
```sql
ALTER TABLE guests ADD COLUMN national_id TEXT;
ALTER TABLE guests ADD COLUMN email TEXT;
ALTER TABLE guests ADD COLUMN nationality TEXT;
ALTER TABLE guests ADD COLUMN notes TEXT;
CREATE INDEX IF NOT EXISTS idx_guests_phone ON guests(phone);
CREATE INDEX IF NOT EXISTS idx_guests_national_id ON guests(national_id);
```

**Migration 081 — hospitality_orders.room_id already exists (v0.42.3
migration 076). No new migration needed for room-service flow — just
wire it through the UI.**

### Services touched

- `services/hospitality.ts::listAvailableRoomsForType(typeId)` — new
  helper that returns rooms in `available` OR `dirty` OR `cleaning`
  status with `room_type_id === typeId`, sorted by number. Returns
  `{ id, room_number, status }[]`. Front desk decides which to assign;
  a `dirty` room comes with a "needs turnaround" pill in the picker.
- `services/hospitality.ts::createBooking` — extend input:
  ```ts
  input: {
    guestName: string;
    phone?: string;
    email?: string;
    nationalId?: string;
    nationality?: string;
    notes?: string;
    roomTypeId: string;
    checkIn: string;
    checkOut: string;
    ratePerNight: number;
    adults?: number;
    userId?: string;
    /** Optional pre-assigned room. When set, check-in uses this room
     *  instead of auto-picking free[0]. */
    preferredRoomId?: string | null;
  }
  ```
  Upserts `guests` by `phone` OR `national_id` so returning guests aren't
  duplicated.
- `services/hospitality.ts::openOrder` — already accepts `roomId`.
  Wire it from the UI (see below).
- `services/hospitality.ts::findGuestByPhoneOrId` — new helper for the
  guest-attachment UX.

### Components

- **`RoomPickerSheet`** — new, `src/components/hospitality/room-picker-sheet.tsx`.
  Right-side Sheet like the ingredient picker. Modes:
  - `mode="booking"` — only shows rooms where status ∈ {available, dirty, cleaning} for the given type; badge dirty ones as "needs turnaround".
  - `mode="room_service"` — only shows rooms where status = 'occupied' (guest currently there); includes guest name from the folio.
  Search by room number, filter chips per status.
- **`GuestPicker`** — inside the booking dialog. Combobox with
  `onCreate` that opens a `GuestFormDialog` for full guest details
  (name, phone, email, national id, nationality, notes). Adopts an
  existing guest when the phone or ID matches.
- **`BookingDialog`** — replace the current `CompactFormDialog` usage
  with a proper form with:
  - Guest picker (search existing OR create new)
  - Room type combobox
  - "Assign room now?" toggle → opens `RoomPickerSheet(mode='booking')`
  - Dates
  - Rate override (defaults to type base rate)
  - Notes
  All in a single Sheet, 500-600px wide.
- **Room-service order flow** — in `HospitalityOrdersPage`, when
  `orderType === 'room_service'` on `newOrder`:
  1. Open `RoomPickerSheet(mode='room_service')`.
  2. On pick, call `openOrder({ orderType, userId, partySize, roomId })`.

### Tasks

- T80: Migration 080 (guests fields) + register in lib.rs
- T81: Service `listAvailableRoomsForType` + `findGuestByPhoneOrId` +
       extend `createBooking` for guest fields + preferredRoomId
- T82: `RoomPickerSheet` component (both modes)
- T83: `GuestPicker` + `GuestFormDialog`
- T84: `BookingDialog` replacing the compact form for bookings
- T85: Rooms tab: KPI tile clicks filter the table (available/occupied etc)
- T86: Room-service order flow uses `RoomPickerSheet` at newOrder time,
       passes roomId to openOrder, folio auto-resolves
- T87: Fix booking dialog `type.base_rate` undefined display
- T88: Bookings table search + arrival/departure filter chips
- T89: `charge-to-room` inline SQL → `listOpenFolios()` service helper

---

## v0.44.1 — Recipe + inventory cleanup + unit conversion

Two entry points to the same recipe (chip dialog + canvas) is causing
data drift. Unit conversion at consumption time is silently over-
deducting. Units subsystem needs to be wired everywhere.

### Services touched

- `services/hospitality.ts::planRecipeConsumption` — before subtracting
  from batches, `convertUnits(recipe_qty, recipe.unit, product.unit)`.
  If conversion returns null (cross-dimension), throw with
  "Ingredient X unit mismatch: recipe expects grams, product stored as
  bunches — reassign in Settings > Units."
- `services/hospitality.ts::menuAvailability` — same conversion applied
  before computing servings.
- `services/inventory.ts::adjustStock`, `services/stock-transfers.ts`,
  `services/purchase-orders.ts` — all should default to `UnitSelect`.

### Components

- Remove chip `RecipeDialog` usage from menu list (row-click detail
  page is the one path). Keep the component file only if it's still
  imported elsewhere.
- Wire `UnitSelect` into:
  - Purchase order line items
  - Stock adjustment dialog
  - Stock transfer form
  - Damage recording

### Tasks

- T90: `convertUnits` integration in `planRecipeConsumption` +
       `menuAvailability` with clear error on cross-dimension
- T91: Remove `Recipe` column from menu list; drop `setRecipeMenu`
       state + `RecipeDialog` mount from `HospitalityMenuPage`. Row
       click → detail page → canvas is the only path.
- T92: `UnitSelect` in purchase-order line item (`src/pages/purchase-orders.tsx`)
- T93: `UnitSelect` in stock adjustment (`components/inventory/*` — hunt existing dialog)
- T94: `UnitSelect` in stock transfers
- T95: `UnitSelect` in damages page
- T96: Delete `/hospitality/recipes` list route (redundant with menu detail page)

---

## v0.44.2 — Housekeeping + folios + shifts

Housekeeping and Rooms are two views of the same data. Folios can
only be created via check-in. Service periods (lunch, dinner shifts)
don't exist yet.

### Data model

**Migration 082 — service periods**
```sql
CREATE TABLE IF NOT EXISTS service_periods (
  id TEXT PRIMARY KEY,
  branch_id TEXT,
  name TEXT NOT NULL,           -- "Lunch", "Dinner", "Breakfast"
  starts_at TEXT NOT NULL,       -- "12:00"
  ends_at TEXT NOT NULL,         -- "16:00"
  active INTEGER NOT NULL DEFAULT 1
);
CREATE TABLE IF NOT EXISTS service_period_sessions (
  id TEXT PRIMARY KEY,
  period_id TEXT NOT NULL REFERENCES service_periods(id),
  opened_at TEXT NOT NULL DEFAULT (datetime('now')),
  closed_at TEXT,
  opened_by TEXT REFERENCES users(id),
  closed_by TEXT REFERENCES users(id),
  gross_sales REAL,
  gross_covers INTEGER
);
```

### Components

- Merge `HospitalityHousekeepingPage` into `HospitalityRoomsPage` (a
  "Housekeeping" tab within Rooms page). Route stays for
  backward-compat but redirects.
- `FoliosPage` gets a "New walk-in folio" button that creates a folio
  attached to a lightweight guest (name + phone only).
- New `SettingsServicePeriods` page under Settings → Hospitality with
  CRUD for lunch/dinner/etc.
- `ServicePeriodOpenClose` widget in Kitchen Display + Orders page
  header showing current shift + open/close button.

### Tasks

- T100: Migration 082 (service periods) + register
- T101: Merge Housekeeping page into Rooms as a tab; add redirect
- T102: Charge-to-Room button uses `listOpenFolios()` service
- T103: Folios page adds "New walk-in folio" flow
- T104: ServicePeriods CRUD in Settings → Hospitality
- T105: Service-period open/close widget on Orders + KDS

---

## v0.44.3 — Cross-cutting polish + Receipts investigation

### Tasks

- T110: Investigate the "Receipts" tab user asked about — locate,
        determine intent (receipt-printing settings vs sale-receipt
        list vs eTIMS receipts). Add the missing add/list surface.
- T111: In-app `/hospitality/kitchen` page becomes a manager summary
        (not a full KDS clone). Or delete + point to the KDS window.
- T112: Order Board debounce reads (query only when at least 500ms
        since last poll) + skip when the tab is hidden.
- T113: Menu-item dialog allergens becomes a chip picker (add/remove
        pre-populated tags: dairy, gluten, nuts, egg, soy, shellfish,
        pork, mustard, sesame) with free-text add.
- T114: Reusable Modifier group library (Sauces, Sides, Doneness) —
        create once in Settings, attach many. New table
        `menu_modifier_group_templates` + link table.
- T115: Titlebar shows menu-item name on `/hospitality/menu/:id`
        instead of just "Omnix Hospitality".
- T116: RoomTypeDetail confirm exists + drill-in works from room-types
        list (v0.40 shipped this but needs verification post-audit).
- T117: `Recipes & Costing` report page (renamed from list) — add
        actual analytical value (top 10 by margin, worst 5 by food
        cost %, cost trend across all recipes).

---

## Ship sequence + rhythm

Build all four sub-ships in order. Tag ONCE at the end as v0.44.0 —
per your "no intermediate versions" policy from the v0.43 batch.

Verification gate before that single tag:
1. `npx tsc --noEmit` clean
2. `npx vitest run --silent` all green (add hospitality-v44 spec)
3. `node scripts/audit-codebase.mjs` — 0 errors
4. Cold-cache `next build` in website/ — clean
5. Manual sanity: create a booking with a picked room, check in, open
   a room-service order for that room, add items, send to kitchen,
   mark served, verify ingredient stock deducted with correct unit
   conversion, close service period.

## Non-scope in this batch (retained for later)

- Windows 11 snap-layouts (from v0.42.6 spec — still no viable plugin)
- Structured floors table (from v0.42.5 spec — draft schema, no UI yet)
- Menu-item variant pricing (small/medium/large)
- Recipe versioning à la Toast
- Batch recipes
- Nutrition info per serving
- Reservation deposits + no-show fees (partial — flagged as M5)
- Loyalty / repeat-guest scoring
