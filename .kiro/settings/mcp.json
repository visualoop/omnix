{
  "mcpServers": {
    "magic": {
      "command": "npx",
      "args": [
        "-y",
        "@21st-dev/magic@latest",
        "API_KEY=\"${TWENTY_FIRST_API_KEY}\""
      ],
      "env": {
        "TWENTY_FIRST_API_KEY": "<ADD_TWENTY_FIRST_API_KEY_HERE>"
      },
      "disabled": true,
      "_note": "Set disabled to false after adding the API key above."
    },
    "better-auth": {
      "type": "http",
      "url": "https://mcp.chonkie.ai/better-auth/better-auth-builder/mcp",
      "_note": "Better Auth's official guided-config MCP. Lets the agent verify config + generate plugin setups against current docs."
    }
  }
}
